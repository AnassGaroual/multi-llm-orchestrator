name: 🔍 Code Quality & Security

# THIS IS A REUSABLE WORKFLOW
on:
  workflow_call:
    inputs:
      skip-tests:
        description: 'Skip test execution'
        required: false
        type: boolean
        default: false
    secrets:
      CODECOV_TOKEN:
        required: false

permissions:
  contents: read
  security-events: write
  pull-requests: write
  checks: write

jobs:
  # Stage 1: Fast fail on formatting (2-3 min)
  formatting:
    name: 🎨 Code Formatting
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Required for ratchetFrom

      - name: ☕ Setup JDK 25
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 25
          cache: gradle

      - name: 🔧 Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          cache-read-only: false
          gradle-home-cache-cleanup: true

      - name: 🎨 Spotless Check
        run: ./gradlew spotlessCheck --no-daemon --console=plain

      - name: 💬 Comment on PR (failure)
        if: failure() && github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `## ❌ Code Formatting Failed

              **Fix it:**
              \`\`\`bash
              ./gradlew spotlessApply
              git add .
              git commit --amend --no-edit
              git push --force-with-lease
              \`\`\`

              **Or trigger auto-fix:** Add label \`spotless-fix\` to this PR.`
            });

  # Stage 2: Security scanning (parallel with formatting)
  security:
    name: 🔒 Security Scan
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: ☕ Setup JDK 25
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 25
          cache: gradle

      - name: 🔧 Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      # Secret scanning
      - name: 🕵️ Gitleaks (Secret Detection)
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # SAST - Static Application Security Testing
      - name: 🛡️ Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: java
          queries: security-extended,security-and-quality

      - name: 🔨 Autobuild
        uses: github/codeql-action/autobuild@v3

      - name: 🛡️ Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v3
        with:
          category: "/language:java"

      # Dependency vulnerability scanning
      - name: 🔍 OWASP Dependency Check
        run: ./gradlew dependencyCheckAnalyze --no-daemon
        continue-on-error: true

      - name: 📤 Upload Security Reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: security-reports
          path: |
            build/reports/dependency-check-report.html
          retention-days: 30

  # Stage 3: Build and test (after formatting passes)
  build-test:
    name: 🧪 Build & Test
    runs-on: ubuntu-latest
    needs: formatting  # Wait for formatting to pass
    if: ${{ !inputs.skip-tests }}
    timeout-minutes: 20

    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4

      - name: ☕ Setup JDK 25
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 25
          cache: gradle

      - name: 🔧 Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          cache-read-only: false
          gradle-home-cache-cleanup: true

      - name: 🔨 Build
        run: ./gradlew build --no-daemon -Dorg.gradle.jvmargs=-Xshare:off

      - name: 🧪 Test
        run: ./gradlew test --no-daemon -Dorg.gradle.jvmargs=-Xshare:off

      - name: 📊 Publish Test Results
        if: always()
        uses: EnricoMi/publish-unit-test-result-action@v2
        with:
          files: build/test-results/test/*.xml
          check_name: Test Results
          comment_mode: off

      - name: 📦 Upload Test Reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-reports
          path: |
            build/reports/tests/test
            build/test-results/test
          retention-days: 7

      - name: 📈 Generate Code Coverage
        if: success()
        run: ./gradlew jacocoTestReport --no-daemon
        continue-on-error: true

      - name: 📤 Upload Coverage to Codecov
        if: success()
        uses: codecov/codecov-action@v4
        with:
          files: build/reports/jacoco/test/jacocoTestReport.xml
          flags: unittests
          fail_ci_if_error: false
          token: ${{ secrets.CODECOV_TOKEN }}
